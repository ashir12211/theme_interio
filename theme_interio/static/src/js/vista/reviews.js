/**
 * Vista Reviews — Interaction
 */
$(document).ready(function () {
    var $reviews = $('.vh-reviews');

    if (!$reviews.length) return;

    // Optional: Add simple scroll reveal if desired
    // Currently relying on CSS hover effects for interactivity.
});
