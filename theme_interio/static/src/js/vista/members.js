/**
 * Vista Members - Animations
 */
$(document).ready(function () {
    var $members = $('.vh-members');

    if (!$members.length) return;

    // Optional: Add simple scroll reveal if desired
    // Using simple CSS hover effects for now
});
